/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'pleydrywdubuhworsurp.supabase.co',
      },
    ],
  },
}

module.exports = nextConfig
